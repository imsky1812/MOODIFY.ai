# ===== MOODIFY.ai Requirements =====
# Cleaned — removed Flask, redis, openai (unused), kept only what the app needs

# ── Web Framework ──
fastapi==0.129.0
uvicorn==0.41.0
starlette==0.52.1
python-multipart==0.0.22

# ── Environment ──
python-dotenv==1.2.1

# ── AI / LLM ──
groq==1.0.0
transformers==5.2.0
torch==2.10.0
tokenizers==0.22.2
safetensors==0.7.0
huggingface_hub==1.4.1

# ── Text Emotion Detection ──
# (DistilRoBERTa via HuggingFace pipeline)
# No extra package needed beyond transformers + torch

# ── Face Emotion Detection ──
deepface==0.0.79
opencv-python-headless==4.8.1.78
retina-face==0.0.17
mtcnn==1.0.0
pillow==12.1.1

# ── Language Detection ──
langdetect==1.0.9

# ── Text-to-Speech (Edge TTS) ──
edge-tts==7.0.2

# ── Audio Processing ──
# ffmpeg is a system dependency (install separately, not via pip)
# Groq Whisper handles STT — no SpeechRecognition needed

# ── Spotify Integration ──
spotipy==2.25.2
requests==2.32.5
requests-oauthlib==2.0.0

# ── Data / Utils ──
pydantic==2.12.5
numpy==1.23.5
scipy==1.15.3
scikit-learn==1.7.2

# ── Async / HTTP ──
httpx==0.28.1
aiohttp==3.13.3
anyio==4.12.1
websockets

# ── Deployment ──
gunicorn==25.1.0
